// Placeholder — profile input screens land in Phase 1 Pass B.
export default function ProfilePage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Profile</h1>
      <p className="mt-2 text-slate-600">
        Your profile lives here. Input screens and local persistence are coming next.
      </p>
    </section>
  );
}
