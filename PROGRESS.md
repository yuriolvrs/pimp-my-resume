# PROGRESS

> Claude Code: read this at the start of every session; update it truthfully at the end. Only mark items done that are implemented AND tested.

## Current phase
Phase 5 — Cover letter (not yet started)

## Phase checklist
- [x] Phase 1 — Foundation: scaffold, types, Dexie, routing, profile screens, export/import/delete-all
- [x] Phase 2 — Proxy & LLM layer (real end-to-end hello call confirmed against Groq via `/dev/llm`)
- [x] Phase 3 — Posting analysis (paste posting → LLM analysis, editable, stored per posting; verified live against Groq)
- [x] Phase 4 — Resume generation (tailored resume, atom-grounded sourceMap, field editing, HTML/print export; verified live against Groq)
- [ ] Phase 5 — Cover letter
- [ ] Phase 6 — LaTeX pipeline
- [ ] Phase 7 — Polish

## Done
- Phase 1 Pass A — Foundation scaffold (verified: `npm run build`, `npm run test`, `npm run dev` all green):
  - Vite + React + TypeScript (strict mode) + React Router.
  - Tailwind CSS configured.
  - Shared types in `src/types/index.ts` transcribed from PRD §6 (single source of truth), with `SCHEMA_VERSION`.
  - Dexie DB skeleton in `src/lib/db.ts`, tables derived from the shared types.
  - Routing shell + nav with placeholder pages (Profile, Jobs, About/Privacy); About page carries the PRD §10 privacy contract.
  - `.gitignore` (excludes secrets/.env/.dev.vars); Vitest wired with one smoke test.
- Phase 1 Pass B — Profile screens + export/import/delete-all (verified: `npm run build`, `npm run test` (18 tests) green, plus a Playwright-driven manual pass through the running app — fill profile → reload → confirm persisted → export → delete-all → import → reload → confirm restored):
  - Reusable list-editing primitives `src/components/EditableList.tsx` (generic add/remove) and `src/components/StringList.tsx` (string-item specialization), used by every repeating section so they behave uniformly.
  - Profile section forms in `src/components/profile/`: `ContactForm`, `SkillsForm`, `ExperienceForm`, `ProjectsForm`, `EducationForm`, `WritingSamplesForm`.
  - `src/lib/profileStore.ts` (load/save the single local profile) and `src/lib/backup.ts` (export/import/delete-all; pure functions `buildBackup`/`validateBackup`/`parseBackup` unit-tested, Dexie-touching wrappers verified manually).
  - `src/components/profile/BackupControls.tsx` — export JSON, import JSON, delete-all with two-step confirmation.
  - `src/pages/ProfilePage.tsx` composes all of the above; autosaves on change (blur-commit for the two large free-text fields to avoid a write per keystroke).
  - Manual verification caught and fixed a real bug: a stale error message could hide a subsequent success message in `BackupControls` (delete-all appeared to silently do nothing if a prior import error was still showing).
  - Added `.claude/skills/verify/SKILL.md` documenting the build/launch/drive recipe for this app.
- Phase 2 — Proxy & LLM layer (verified: `npm run test` — 31 frontend tests incl. 13 new — and `worker`'s own `npm run test` — 11 tests — both green; `npm run build` and `worker`'s `tsc --noEmit` both typecheck clean; `wrangler dev` + curl + a Playwright pass through the real `/dev/llm` page confirmed the whole chain reaches the real Groq API):
  - New `worker/` package (self-contained, zero runtime deps): `src/index.ts` (single stateless `POST /generate`, forwards to Groq with the key injected server-side, never logs bodies) and `src/lib.ts` (pure CORS/oversize/rate-limit helpers, unit-tested in `src/lib.test.ts`). Config in `wrangler.toml` (Groq base URL/model confirmed against official docs, allowed origins); secret `LLM_API_KEY` is *not* set yet (see below).
  - `src/lib/llm.ts` — the one provider-agnostic module (CLAUDE.md invariant); `generate()`/`generateStructured()`, never holds a key, talks only to the proxy.
  - `src/lib/json.ts` — defensive JSON parsing for structured LLM output (strip fences → parse → fall back to substring extraction → validate shape).
  - `src/pages/DevLlmPage.tsx` at unlinked route `/dev/llm` — manual "hello" test harness, not linked in nav, not part of the product.
  - `.env.example` (`VITE_PROXY_URL`) and `worker/.dev.vars.example` (`LLM_API_KEY`) document the env vars needed; `.gitignore` updated to keep the `.example` files trackable while still ignoring the real secret files.
  - **Verified without a real key:** with no `LLM_API_KEY` set, the worker still correctly forwarded a request to the real `https://api.groq.com/openai/v1/chat/completions` and Groq replied with a genuine `401 invalid_api_key` — proving the full request path (app → worker → provider) is wired correctly. CORS allow/deny and oversized-payload rejection (413) also confirmed live via curl.
- Phase 3 — Posting analysis (verified: `npm run test` — 49 tests, 18 new — and `npm run build` both green; a Playwright pass through the real running app, against the real Groq API via the local Worker, drove the full flow: paste posting → save → Analyze → edit a keyword → reload → confirm persisted → posting list shows "Analyzed" → visited an unknown `/jobs/:id` and confirmed the not-found state):
  - `src/prompts/analyzePosting.ts` (first file in `src/prompts/`) — `buildAnalyzePostingPrompt`, `serializeProfileForPrompt` (profile rendered as compact plain text so the model can quote it verbatim as evidence; `writingSamples` and contact details omitted), `isJobAnalysis` type guard (structural, element-type-checked so malformed shapes are caught and retried by `generateStructured` rather than reaching the UI). Both variable inputs are length-capped and truncated independently before assembly, keeping requests comfortably under the Worker's 100KB body limit. Unit-tested in `analyzePosting.test.ts`.
  - `src/lib/jobStore.ts` — mirrors `profileStore.ts`'s pattern for the multi-record `jobPostings` table: `newJobPosting`, `listJobPostings`, `loadJobPosting`, `saveJobPosting`, `deleteJobPosting`, and pure `postingLabel` (unit-tested in `jobStore.test.ts`).
  - `src/lib/profileStore.ts` — added `hasProfileContent`, gates the Analyze button so a blank profile can't be sent to the LLM.
  - `src/components/jobs/AnalysisEditor.tsx` — the analysis is fully editable (a deliberate choice, not just PRD-minimum "rendered"), built entirely from the existing `StringList`/`EditableList` primitives so a bad or incomplete LLM extraction can be corrected by hand before later phases consume it.
  - `src/pages/JobsPage.tsx` (rewritten) — paste + save a new posting, list saved postings newest-first. `src/pages/JobDetailPage.tsx` (new, route `/jobs/:id`, the app's first route param) — edit posting text, run/re-run analysis, edit the result, delete the posting (two-step confirm, matching `BackupControls`).
  - No changes needed to `src/types/index.ts`, `src/lib/db.ts`, or `src/lib/backup.ts` — the Phase 3 shapes, Dexie table/index, and backup export/import already existed from Phase 1.
  - **Real-model finding, addressed:** live testing against `llama-3.1-8b-instant` showed it would sometimes return `profileEvidence` as a bare string instead of an array — correctly rejected by `isJobAnalysis` and retried by `generateStructured`, but a real failure mode. Strengthened the prompt with an explicit array-shape reminder and example; confirmed reliable (multiple live runs) afterward. Residual, expected model-quality noise (e.g. occasionally mis-classifying a requirement, or leaving `profileEvidence` empty) is exactly why the analysis is editable rather than read-only — not a code bug, and not something a stricter guard should paper over. Programmatic verbatim-evidence checking is intentionally deferred to Phase 4's `sourceMap` work, per the PRD.

- Visual redesign (verified: `npm run build` and `npm run test` — 49 tests — both green; Playwright screenshots of Profile, Jobs, and Job Detail compared against the source design):
  - Ported a Figma Make mockup (`Job Application Assistant/`, Figma project "Job Application Assistant") into the existing pages — same Dexie-backed logic and routing, restyled markup only. The mockup's own code (unused shadcn/Radix/MUI boilerplate) was not adopted; only its Tailwind utility styling and `lucide-react` icons were.
  - New `src/components/ui/primitives.tsx` (`Card`, `Btn`, `Badge`, `SectionTitle`, `FieldInput`, `FieldTextarea`) — the one shared look-and-feel module every page and form now builds on.
  - `EditableList`/`StringList` restyled to the new card/chip idiom, which cascaded the new look through every profile section form and `AnalysisEditor` without touching their logic.
  - Self-hosted the Outfit font (`src/assets/fonts/`) instead of loading it from `fonts.googleapis.com`, to avoid a non-LLM network call from the privacy-first app.
  - Caught and fixed before commit: the mockup export's `worker/.dev.vars.example` diff (from unrelated in-progress work) contained a live Groq key pasted in place of the placeholder; reverted to the placeholder and the user revoked the key.

- Phase 4 — Resume generation (verified: `npm run build` and `npm run test` — 97 tests, 22 new — both green; a Playwright pass through the real running app, against the real Groq API via the local Worker, generated a resume from a seeded profile/matched posting, confirmed correct contact/summary/skills/education passthrough and rewritten experience/project bullets, reloaded to confirm Dexie persistence, exercised the two-step Regenerate confirm, and confirmed the Export PDF button and hidden print view are present):
  - Per user decisions up front: one resume generation per posting (overwritten on regenerate, deterministic `${jobPostingId}:resume` id in `genStore.ts`, matching the matching screen's "Re-run" pattern); evidence pool prioritizes confirmed requirement matches + `additionalInfo`, falling back to the rest of the profile's own atoms when a section would otherwise be thin; length/section shape (single page, ~3-5 bullets/role, ~4/project, 2 projects shown) taken from a user-provided Jake's-Resume-style `.tex` as a prompt guide only (no LaTeX parsing — that's Phase 6); over-claiming handled via a tighter prompt constraint only, per explicit instruction.
  - `SourceMapEntry.profileEvidence: string[]` (free text) → `atomIds: string[]` (references into `ProfileAtom`), matching `RequirementMatch`'s shape — the Phase 3→4 migration flagged in `PHASE4_PLAN.md`. No other free-text evidence fields introduced.
  - `src/prompts/generateResume.ts` — prompt builder, response validator, and `candidatesForLabel`/`legitimizeBullet` evidence-scoping helpers for **one resume section at a time** (one experience or project entry), not one call for the whole resume. `src/lib/generation/runResumeGeneration.ts` orchestrates one such call per section with limited concurrency (`mapWithConcurrency`, exported from `lib/matching/runMatching.ts` for reuse), falls back to a profile entry's own original bullets when nothing is generated for it (no sourceMap entry for those — they're verbatim, not generated), and ranks/caps projects to `MAX_PROJECTS` by how many bullets were generated for each.
  - **Real-model finding, addressed (significant):** an initial one-call-for-the-whole-resume design was tried first and found unreliable against the free `llama-3.1-8b-instant` model — it would fall into a repetition loop (near-identical bullets generated dozens of times) and exhaust its token budget mid-JSON, failing almost every attempt even with explicit anti-repetition prompt wording. Fixed structurally, not just with more prompt text: split into one focused call per resume section (mirroring the exact fix that made Phase 3's matching reliable — one call per requirement instead of one big analysis call), which removed the cross-entry reasoning burden and made each response small enough to finish cleanly. A second, distinct failure mode surfaced even after that split: the model would sometimes answer with several alternative JSON attempts concatenated together (separated by "or" or blank lines) instead of committing to one. Fixed at the shared parser level (not just prompt wording) — `src/lib/json.ts`'s `extractJsonSubstring` was rewritten from a naive "first open bracket to last close bracket" span (which would swallow every alternative as one invalid blob) to a proper balanced-depth scan that stops at the first complete JSON structure, correctly skipping braces/brackets inside string values. Confirmed via direct repeated calls against the real API: first-attempt validity went from ~10% (whole-resume call) to ~60-90% (per-section call, before vs. after the parser fix), with `generateStructured`'s existing one-retry-on-failure further covering the remainder — consistent with Phase 3's documented tolerance for residual small-model noise, not a masked bug.
  - `src/lib/genStore.ts` (mirrors `jobStore.ts`'s pattern) — `newGeneration`/`saveGeneration`/`loadGeneration`/`listGenerationsForPosting` against the already-declared `generations` Dexie table (unused since Phase 1 scaffold).
  - `src/components/resume/ResumeEditor.tsx` reuses the Profile page's own form components (`ContactForm`, `SkillsForm`, `ExperienceForm`, `ProjectsForm`, `EducationForm`) directly, since a `ResumeContent` snapshot has identical field shapes — per CLAUDE.md's "search for an existing pattern before writing a duplicate." `StringList` gained an optional `itemBadge` render prop (used to show an "Unevidenced -- not traceable to your profile" badge above any bullet whose sourceMap entry has zero `atomIds`, matched by exact generated text — a bullet the user hand-edits no longer matches and stops being flagged, which is intentional: editing it is the user taking ownership); `ExperienceForm`/`ProjectsForm` gained an optional passthrough `bulletBadge` prop for this, unused (and behavior-unchanged) on the Profile page itself.
  - `src/components/resume/ResumePrintView.tsx` — plain HTML/Tailwind print layout (`hidden print:block`, `@page` CSS), no PDF library; `src/pages/ResumePage.tsx` (new route `/jobs/:id/resume`) ties generation/editing/regenerate/export together; `JobDetailPage` gained a "Generate resume" entry point next to "View matches" once matches exist.

- Phase 4 correction — resume generation rewritten as selection-only, no LLM call (verified: `npm run build` and `npm run test` — 93 tests — both green; live Playwright pass confirmed a matched bullet moves to the top of its section with its text 100% unchanged and a "Matched to this job" badge, and that print media hides the app's nav entirely):
  - **Real fabrication found in a live-generated resume**, not a hypothetical: the per-section LLM design above rewrote "Planned and executed events as project head..." (an events-organizing role) into "Developed scalable and efficient code using Java and Spring Framework, with experience in Spring MVC and Spring Boot microservices" -- backed only by the word "JavaScript" in additionalInfo. A second bullet similarly invented "REST architecture and distributed caching" for a billing-system role never described that way. The anti-fabrication guard only caught invented durations/quantities, not invented technologies/claims -- this was resume fraud, not a quality nit.
  - Per explicit user direction: resume generation no longer rewrites or invents any text. `src/lib/generation/selectResumeContent.ts` replaces `runResumeGeneration.ts`/`generateResume.ts` entirely (both deleted, along with their prompts and tests) with a pure, synchronous function -- every bullet and skill in the output is copied verbatim from the profile; matching only reorders items within each section (matched-to-a-requirement first, stable order otherwise) and ranks/caps projects by matched-bullet count. No LLM call at all in this path anymore, so the whole class of reliability/fabrication problems from the per-section design is moot. `SourceMapEntry`/the "badge" concept were repurposed from a fabrication *warning* (amber, shown when ungrounded) to a positive "Matched to this job" indicator (blue, shown when grounded) -- there's nothing left to warn about.
  - `Profile.skills` changed from a flat `string[]` back to `SkillGroup[]` (`{ category, items }[]`) per explicit instruction, reversing an earlier flattening decision -- categories are user-named and editable both on the Profile page (`SkillsForm.tsx` rewritten to a repeating category-group editor, mirroring `ExperienceForm`'s entry-list pattern) and reflected in the printed/exported resume (grouped under each category heading, matching the user's `.tex` template's Languages/Frameworks/etc. layout instead of one flat comma list). `profileStore.ts`'s `normalizeSkills` (now exported and unit-tested) migrates both the old flat-list shape and the current grouped shape safely on load.
  - Fixed a real, separate print-output bug found from a screenshot of an actual exported PDF: the app's own nav header (logo, "LOCAL ONLY" badge, Profile/Jobs/About links) was bleeding into the printed page because `App.tsx`'s `<header>` had no `print:hidden`. Fixed, plus `<main>` gets `print:p-0 print:max-w-none` so the print layout isn't boxed in by the app shell's own width/padding. Also sets `document.title` to the candidate's name while the Resume page is open, so *if* the user leaves the browser's print "headers and footers" option on, it reads their name instead of "Pimp My Resume" -- that option itself (which is what put the URL/date/page-count in the screenshot the user shared) is OS/browser print-dialog chrome with no CSS override; explained to the user rather than silently left unaddressed.
  - Explicitly deferred, per user's own answer: actual LaTeX generation (Phase 6) is not built yet -- this session only fixed the HTML/print export. Per PRD, Phase 6 exports a filled `.tex` file for the user to compile themselves (e.g. Overleaf), not an in-app compiled PDF; an in-app compiled PDF would need a client-side LaTeX engine evaluated as a new dependency first.
  - **Found and fixed immediately after, from the user hitting it live:** opening the Resume page blank-screened (a real React render crash, not just an empty state) whenever a resume had already been generated *before* this session's skills-categories rewrite -- that old-saved `Generation.content.skills` was a flat `string[]`, and `SkillsForm`/`ResumePrintView` now expect `SkillGroup[]`, so `<TagInput>` threw reading `.length` on a plain string. Reproduced first via Playwright with a seeded stale-shape generation (confirmed the exact crash + component stack), then fixed with `genStore.ts`'s new `migrateGeneration`, which reuses `profileStore.ts`'s existing `normalizeSkills` (already built to handle both shapes) so any generation saved under the old schema loads and displays correctly instead of crashing. Unit-tested (3 new tests) and reconfirmed fixed live via the same reproduction script -- console errors went from several uncaught render exceptions to none.
  - **Found and fixed right after that, from the user showing a screenshot of a resume still displaying old fabricated text** (the exact "Developed scalable and efficient code using Java and Spring Framework..." bullet from the finding above) with a "Matched to this job" badge on it: the skills-shape crash fix made the page render again, but didn't check whether the bullet *text* stored from before this correction was actually real -- a pre-correction `Generation` still had the fabricated bullets baked into `content.experience[].bullets` itself, and there's no way to "migrate" invented text back into real text. Added `isResumeContentVerbatim(content, profile)` to `selectResumeContent.ts` -- checks every bullet/skill in a stored generation against the full set of real text in the profile, catching exactly this failure mode (invented text) even though it can't detect the milder case of text stolen from a different profile entry. `ResumePage.tsx` now checks this on load: a generation that fails it is never rendered as if it were trustworthy -- instead of the editor, the user sees a plain "This resume needs to be rebuilt" notice explaining why, with a one-click "Rebuild resume" action (no confirm needed, since there was nothing trustworthy in it to lose). 4 new unit tests (13 total in that file) and build both green; reconfirmed live via Playwright using the user's own reported scenario -- seeded the exact stale/fabricated generation, confirmed the fabricated text never renders anywhere on the page, confirmed the rebuild notice and button appear, then confirmed clicking "Rebuild resume" replaces it with the real bullet verbatim and restores the normal Regenerate/Export PDF header.

## Known issues / open questions
- Not yet deployed. When ready: `wrangler login`, `wrangler secret put LLM_API_KEY` (from `worker/`), `npx wrangler deploy`, then add the deployed Cloudflare Pages origin to `ALLOWED_ORIGINS` in `worker/wrangler.toml`.
- The worker's in-memory rate limiter is per-isolate and resets on cold start/redeploy — basic abuse dampening, not a hard global guarantee. Fine for a personal-use proxy; documented in `worker/src/lib.ts` if it ever needs upgrading to KV/Durable Objects.

## Session log
(append one line per session: date — what was done)
- 2026-07-22 — Job matching redesign: split posting analysis into extraction (roleSummary/requirements-with-severity/keywords only, `src/prompts/analyzePosting.ts`) and a separate two-step matching pass (`src/lib/matching/`: synonym+token-overlap candidate retrieval with no LLM call, then a focused per-requirement LLM verification via `src/prompts/matchRequirement.ts` that can only select from candidate atom ids — never free text). Profile is indexed into `ProfileAtom[]` on demand (`src/lib/profileAtoms.ts`); `JobAnalysis.matches` now carries one `full`/`partial`/`gap_no_candidates`/`gap_unverified` entry per requirement, referencing atoms by id. Deleted `sanitizeJobAnalysis` (the old substring-verification safety net) entirely — no longer needed since evidence can't be hallucinated. New master-detail Matching review screen (`/jobs/:id/match`, `src/pages/MatchingReviewPage.tsx`) with reject/swap per matched atom, an explicit empty state + add-evidence flow for gaps, and an always-visible "Additional information" section (`Profile.additionalInfo`, new). `SCHEMA_VERSION` bumped to 2 (no migration for old local analyses, per explicit user decision — they just re-run). Unit tests (91 total, ~18 new: profileAtoms, retrieval, matchRequirement, runMatching, updated analyzePosting) and build both green. Verified live end-to-end against the real Groq API via Playwright: extraction correctly split required/preferred and excluded responsibilities; a requirement phrased "3+ years of experience with the JVM ecosystem" against a bare "Java" skill correctly retrieved the candidate via the synonym table and the LLM verification conservatively judged it unconfirmed (gap_unverified, not a crash or false positive) — reject/swap/add-evidence/Additional-information all exercised live and persisted correctly across reload. Caught and fixed a real race condition during this verification: `handleConfirmMatching` was persisting matches via the fire-and-forget `update()` helper and navigating immediately, so the Matching review page's fresh Dexie read could land before the write committed, observed live as `matches: []`; fixed by awaiting `saveJobPosting` directly before navigating.
- 2026-07-22 — Matching UI follow-up: "Add evidence" is now always available on the Matching review screen, even when a requirement already has confirmed evidence — it appends an additional atom (any source: skills, experience, or project bullets) via `addEvidence` rather than only being offered for gaps. The "Additional information" section's add flow now also uses the atom picker (select an existing skills/experience/projects atom, or write new text) instead of free-text-only, and excludes any atom already used as evidence for some requirement (computed from the union of all `RequirementMatch.atomIds`) so the picker only offers not-yet-used items. Build + all 73 tests green; verified live against the real Groq API via Playwright — added a second matched atom to an already-"Full match" requirement (kept status, both atoms shown independently with their own Swap/Reject), then confirmed the Additional-information picker showed only the one untouched skill and hid the three atoms already attached to requirements.
- 2026-07-22 — "Add evidence" modal: replaced the inline `AtomPicker` used for the requirement-level "Add evidence" action with a centered modal (new `Modal` primitive in `src/components/ui/primitives.tsx` -- plain CSS fade/scale transition, no animation library; new `src/components/jobs/EvidenceModal.tsx`), styled as condensed versions of the Profile page's Skills/Work Experience/Projects sections (chip cloud for skills; role+company or project name as a card header with bullets listed underneath, no date fields or form chrome). Education is intentionally excluded from this picker. Header (requirement text + condensed "Current evidence") and footer (write-new-evidence input) stay pinned while the middle browsing area scrolls, same as before. Behavior correction from the prior drawer version, per explicit instruction: atoms already used as evidence for *other* requirements are no longer dimmed/disabled here (the same evidence can legitimately support more than one requirement) -- that exclusion now applies *only* to the Additional-information picker, which is unchanged. An atom already attached to the *currently open* requirement still shows disabled/labeled "Added" to prevent a duplicate click. Swap and Additional-information continue to use the original inline `AtomPicker`. Build + all 73 tests green; verified live against the real Groq API via Playwright, including adding an Education entry to the profile and confirming no "Education" heading or its content ever appears in the Add-evidence modal, and confirming a bullet already matched to a different requirement showed selectable (not dimmed) and could be added as a second requirement's evidence too.
- 2026-07-22 — Stop discarding manual matching edits on navigation: matches were already persisted correctly on every edit (autosave via `saveJobPosting`, unchanged) -- the actual bug was `JobDetailPage`'s "Confirm & run matching" button unconditionally re-running the whole matching pass (silently overwriting any reject/swap/add-evidence edits) every time the user returned to the posting and clicked it again, since it was the only way back into the Matching screen. Fixed: the button now only runs matching the first time (`posting.analysis.matches.length === 0`); once matches exist it just navigates, relabeled "View matches". Added an explicit "Re-run matching" action on the Matching review screen itself (two-step confirm, matching the app's existing delete-posting/delete-all pattern, since it intentionally overwrites manual edits) so re-matching is always available without going back through Job Detail. Build + all 73 tests green; verified live against the real Groq API via Playwright -- rejected an auto-match, navigated away and back, confirmed the button read "View matches" (not the old "Confirm & run matching") and the manual reject survived, then used the new "Re-run matching" button and confirmed it correctly restored the auto-match.
- 2026-07-22 — Posting Text auto-resize + Matching screen "Evidence in use": `JobDetailPage`'s Posting Text textarea now grows to fit its full content (ref + `scrollHeight`-driven inline height on every text change) instead of scrolling inside a `max-h-[70vh]` box. Found and fixed a real bug while verifying live: the resize effect's dependency (`posting.rawText`) becomes available before `profile` finishes loading, so it fired once while the component was still rendering its "Loading…" placeholder (textarea not yet mounted, ref null) and never fired again once the real content mounted; fixed by also depending on a `postingReady` flag (`Boolean(posting && profile)`). Separately, added a new full-width "Evidence in use" section to `MatchingReviewPage` (`src/pages/MatchingReviewPage.tsx`), sitting below the two-pane layout (belongs to neither pane): lists every profile atom currently attached as evidence to at least one requirement, with a source badge, its text, and a "matched N×" count; clicking a row expands it (collapsed by default) to show which requirement(s) it's attached to. Computed live via `useMemo` off `posting.analysis.matches`, so reject/swap/add-evidence on the requirement detail pane above updates it immediately, no reload. Moved the existing "Additional information" section into the same full-width block (unchanged functionally) with a divider between the two subsections, replacing its old placement directly under the selected requirement's detail pane. Build + all 73 tests green; verified live against the real Groq API via Playwright -- confirmed the textarea fits a long real posting with no internal scrollbar, attached one skill as evidence to two requirements and confirmed "Evidence in use" showed "matched 2×" and expanded to list both, then rejected one and confirmed the count/list updated live without a reload.
- 2026-07-22 — Unified evidence picker, deleted the old scrolling `AtomPicker`: generalized `EvidenceModal` (`src/components/jobs/EvidenceModal.tsx`) with `title`/`subtitle`/`confirmedAtoms`/`confirmedLabel`/`newEntryPlaceholder`/`closeOnSelect` props so the same condensed Skills/Work Experience/Projects modal now backs every evidence-picking moment on the Matching screen — Add evidence, Swap, and Additional information's Add accomplishment. Swap opens it in single-pick mode (`closeOnSelect`, title "Swap evidence", pinned "Replacing" summary of the atom being replaced, list excludes all of the requirement's other currently-confirmed atoms) instead of the old inline expand-in-place `AtomPicker`. Additional information's Add accomplishment now opens the same modal (title "Add accomplishment", "Or write new accomplishment…" placeholder, atoms pre-filtered to skills/experience/projects not already used by any requirement -- same exclusion rule as before, just reusing the shared component instead of a separate search-list widget). Deleted `src/components/jobs/AtomPicker.tsx` entirely -- nothing references the old scrolling search-box picker anymore. Build + all 73 tests green; verified live against the real Groq API via Playwright -- swapped a requirement's evidence from Java to Python and confirmed the modal auto-closed after the single pick, then opened Add accomplishment and confirmed it showed only the now-unused Java skill (Python correctly excluded since it just became a requirement's evidence via the swap).
- 2026-07-22 — Additional information no longer offers atoms it already contains: `additionalInfoCandidates` in `MatchingReviewPage` was excluding atoms used by a requirement match but not atoms already copied into `profile.additionalInfo` itself, so a skill/bullet stayed clickable in the Add-accomplishment modal after being added, letting the same text get duplicated on a second click. Added a second exclusion (`!additionalInfoTexts.has(atom.text)`, where `additionalInfoTexts` is a Set built from `profile.additionalInfo`). Build + all 73 tests green; verified live -- clicked an unused skill in Add accomplishment, watched the modal immediately show "Nothing in your profile yet" (it was the only eligible atom), and confirmed it stayed excluded after closing and reopening the modal fresh.
- 2026-07-22 — Additional information now greys out already-used atoms instead of hiding them, per explicit instruction to match the requirement picker's treatment exactly. `EvidenceModal` gained a `disabledAtomIds` prop, separate from `confirmedAtoms` (which drives the pinned "current" summary header) -- both feed the same dim/"Added" styling, but `disabledAtomIds` doesn't add anything to that header, since Additional information has no single "current" item to summarize. `MatchingReviewPage` now passes the *full* skills/experience/projects atom list to the Add-accomplishment modal instead of pre-filtering it, with `disabledAtomIds` computed as the atoms already used by a requirement or already copied into `profile.additionalInfo`. Build + all 73 tests green; verified live -- opened Add accomplishment and saw Java (already requirement evidence) shown dimmed "ADDED" right alongside selectable Python, then clicked Python and watched it dim to "ADDED" in place too, both staying visible rather than disappearing.
- 2026-07-22 — Job Detail page layout: moved the Re-analyze/matching action buttons off their own standalone row above the two-column grid and into a header bar inside the analysis results, mirroring "Posting Text"'s own card header on the left (an "ANALYSIS" label paired with the actions, instead of an unlabeled button row floating above both columns). The whole analysis results area (previously loose `AnalysisEditor` output sitting directly in the column) is now wrapped in one `Card` so Role Summary/Required/Preferred/Keywords render as nested cards inside it, matching the reference design's card-in-card structure. Re-analyze is now an icon-only button (`Btn` gained an `ariaLabel` prop for this, passed through as `aria-label` since there's no visible text) instead of a text+icon pill. Purely a JobDetailPage layout change -- `AnalysisEditor` itself, the CTA state before any analysis exists, and all matching-screen behavior are untouched. Build + all 73 tests green; verified visually via Playwright screenshot against the reference layout.
- 2026-07-19 — Phase 1 Pass A: scaffolded Vite+React+TS+Tailwind+Router, shared types, Dexie skeleton, routing shell; build/test/dev verified.
- 2026-07-22 — Job Detail page: restyled the analysis results into a card header ("ANALYSIS" label + icon-only Re-analyze button + primary matching action, mirroring "Posting Text"'s own header; `Btn` gained an `ariaLabel` prop for the icon-only case) with Role Summary/Required/Preferred/Keywords nested inside as sub-cards. Then tried making the two panels independently fixed-height/scrollable (`lg:sticky lg:top-20 lg:h-[calc(100vh-7rem)]` + `overflow-y-auto`), but this made scrolling feel wrong -- diagnosed live and reverted per explicit instruction ("just one scrolling"): only the Posting Text card had `lg:sticky lg:top-20` (Analysis never did), and since both columns share one CSS grid row (row height = tallest column's content, regardless of `items-start`), the shorter Posting Text panel would pin itself near the top while scrolling past the taller Analysis column, then abruptly detach once its own (differently-sized) box passed the sticky offset -- an asymmetric, jarring interaction, not smooth independent scrolling. Fix: removed `lg:sticky lg:top-20` from Posting Text entirely; both columns are now plain, non-sticky, independently-sized blocks and the whole page scrolls as one column again, matching the original pre-scroll-experiment behavior. Build + all 73 tests green; verified live via Playwright -- scrolled the page 1000px with mismatched panel content lengths and confirmed "Posting Text" scrolled out of view along with the rest of the page instead of pinning.
- 2026-07-19 — Phase 1 Pass B: profile input screens (contact/summary/skills/experience/projects/education/writing samples) with Dexie autosave, JSON export/import, delete-all; unit tests + Playwright manual verification, fixed a message-hiding bug found during verification. Phase 1 complete.
- 2026-07-19 — Phase 2: built Cloudflare Worker proxy (worker/), provider-agnostic src/lib/llm.ts + src/lib/json.ts, /dev/llm test harness. Verified locally end-to-end against the real Groq API (401 with no key, proving the path works); real hello call is a documented manual follow-up once the user adds a key. Also codified the file-header-comment convention into CLAUDE.md and applied it to every new file.
- 2026-07-21 — Phase 2 closed out: user added a real Groq key to worker/.dev.vars, started `wrangler dev` alongside the frontend, and confirmed a genuine `{"hello": "world"}` response via /dev/llm (200 OK in worker logs). Phase 2 complete; Phase 3 (posting analysis) is next per PRD.md.
- 2026-07-21 — Phase 3: posting analysis. New src/prompts/ (analyzePosting.ts), src/lib/jobStore.ts, /jobs list + /jobs/:id detail routes, AnalysisEditor (fully editable, per user's explicit choice). Unit tests (49 total, 18 new) and build both green. Live Playwright run against the real Groq API caught a real model failure mode (profileEvidence returned as a string, not array) which the guard correctly rejected and retried; fixed by strengthening the prompt, then reconfirmed working. Phase 3 complete; Phase 4 (resume generation) is next per PRD.md.
- 2026-07-21 — Visual redesign: ported a user-provided Figma Make mockup's styling (not its code) onto the existing Profile/Jobs/Job Detail pages. Added shared `src/components/ui/primitives.tsx`, restyled `EditableList`/`StringList` (cascading to every form), self-hosted the Outfit font, added `lucide-react`. Build + all 49 tests green; verified visually via Playwright screenshots against the source design. Found and fixed a live secret (Groq key) that had been pasted into `worker/.dev.vars.example` by unrelated in-progress work; user revoked it.
- 2026-07-24 — Phase 4: resume generation. Migrated `SourceMapEntry` to atom-id references (`PHASE4_PLAN.md`'s flagged Phase 3→4 follow-up). Confirmed four open decisions with the user up front (one-generation-overwrite, evidence-pool-then-full-profile-fallback, length/section shape from a user-provided `.tex` used only as a prompt guide, over-claiming via tighter prompt wording only) before writing any code. Built `src/prompts/generateResume.ts` + `src/lib/generation/runResumeGeneration.ts` (one LLM call per resume section), `src/lib/genStore.ts`, `ResumeEditor.tsx` (reusing the Profile page's own form components), `ResumePrintView.tsx`, and `ResumePage.tsx` at new route `/jobs/:id/resume`. A first one-call-for-the-whole-resume design proved unreliable live (repetition loop, mid-JSON truncation) even after several rounds of prompt hardening; fixed structurally by splitting into one call per section (same fix that made Phase 3's matching reliable) and by rewriting `json.ts`'s substring-extraction fallback to a proper balanced-brace scan (the model would sometimes concatenate several alternative JSON attempts instead of committing to one). Unit tests (97 total, 22 new) and build both green; verified live against the real Groq API via Playwright end-to-end (seeded profile + matched posting → generated resume with correctly-passed-through contact/summary/skills/education and rewritten experience/project bullets → reload persistence → two-step Regenerate confirm → Export PDF/print-view present). Phase 4 complete; Phase 5 (cover letter) is next per PRD.md.
- 2026-07-24 — Moved the "Generate resume" entry point off Job Detail and onto the Matching review screen instead, per explicit instruction, placed directly below the "Evidence in use"/"Additional information" card so it comes after reviewing evidence rather than before. Removed it from `JobDetailPage` entirely (single entry point, not duplicated). Verified live via Playwright: confirmed the button is absent on Job Detail and present in its new spot, and that clicking it navigates to `/jobs/:id/resume`.
- 2026-07-24 — Phase 4 correction: user reviewed an actual exported backup JSON and found real fabrication -- the per-section LLM design (above) had rewritten an events-organizing role's bullets into invented Java/Spring technical claims backed by nothing but the word "JavaScript" in additionalInfo. Per explicit instruction, resume generation was rewritten from the ground up as selection-only: `src/lib/generation/selectResumeContent.ts` (pure, synchronous, no LLM call at all) replaces `generateResume.ts`/`runResumeGeneration.ts` (deleted along with their tests) -- every bullet/skill in the output is copied verbatim from the profile, matched-to-a-requirement items just get reordered to the top and projects get ranked/capped by matched-bullet count. `ResumeEditor`'s badge flipped from an amber fabrication *warning* to a blue "Matched to this job" indicator, since there's nothing left to warn about. Also reintroduced `Profile.skills` as editable category groups (`SkillGroup[]`, reversing an earlier flattening decision) per explicit instruction -- `SkillsForm.tsx` rewritten as a category-group editor, `normalizeSkills` (now exported, unit-tested) migrates old flat-list data safely, and the printed/exported resume groups skills under their categories to match the user's `.tex` template. Separately fixed a real print-CSS bug visible in an actual exported PDF screenshot: the app's own nav header was bleeding into the printed page (`App.tsx`'s `<header>` had no `print:hidden`); fixed, and `document.title` now becomes the candidate's name while the Resume page is open. Explained to the user that the browser's own print header/footer (URL, date, page count) is OS/browser chrome with no CSS override -- unchecking "Headers and footers" in the print dialog is the only fix for that part. LaTeX generation (Phase 6) explicitly deferred per the user's own choice this session. Unit tests (93 total) and build both green; verified live via Playwright -- confirmed a matched bullet moves to the top of its section with its exact original wording and a "Matched to this job" badge, confirmed the Profile page's two skill categories render and edit correctly, and confirmed print-media emulation shows only the resume content with the app nav completely hidden.
- 2026-07-24 — Fixed a blank-page crash reported immediately after the correction above: opening the Resume page threw inside `<TagInput>` (uncaught, blank-screening the whole page) whenever a resume generation already existed from before the skills-categories rewrite, since its saved `content.skills` was still the old flat `string[]`. Reproduced first via a Playwright script seeding that exact stale shape (confirmed the crash and full component stack), then fixed with `genStore.ts`'s new `migrateGeneration`, reusing `normalizeSkills` (already built to handle both shapes) so old-saved resumes load correctly instead of crashing. 3 new unit tests, build green, reconfirmed fixed live via the same reproduction (zero console errors, skills correctly wrapped into a single migrated category).
- 2026-07-24 — Matching-quality fixes, diagnosed from a real exported backup where every requirement on a marketing posting came back `gap_no_candidates` despite plausible profile evidence existing. Two root causes, both fixed: (1) `analyzePosting.ts`'s extraction prompt told the model to pull education/degree requirements out of postings, but `runMatching.ts` has always excluded education atoms as evidence (a degree isn't proof of a skill) -- so any degree requirement was structurally guaranteed to be a permanent, unfixable gap. Removed "education" from the extraction rule with an explicit instruction not to pull degree requirements at all. (2) `retrieval.ts`'s local candidate retrieval is pure lexical token overlap tuned against tech-job examples (Java/Spring/k8s in `synonyms.ts`) with no vocabulary for other domains, so e.g. "Proficient in PowerPoint and Excel" scored zero overlap against a "Microsoft Office" skill and never reached the LLM verification step at all. Added office/marketing/creative synonym phrase groups to `synonyms.ts` (MS Office suite, Google Workspace, e-commerce/digital-marketing, SEO/SKU, digital-content/creative-development incl. Canva/photography/graphic design), fixed `phraseMatchBonus` to normalize non-ASCII punctuation before matching (a job posting's "e‑commerce" used U+2011, not a plain hyphen, so a literal `-` synonym term silently never matched), and loosened `DEFAULT_SCORE_FLOOR` (0.2→0.12) / raised `DEFAULT_TOP_K` (5→8) so weaker-but-real overlaps and broader requirements get a fair shot at LLM verification instead of being filtered out locally. Verified with new unit tests reproducing the exact reported cases (PowerPoint/Excel → Microsoft Office, digital-content → Canva/Photography, e‑commerce with the real non-ASCII hyphen → an "E-Commerce Operator" experience atom) -- all pass, plus all 103 pre-existing tests still green (no regression on the Java/Spring/Kubernetes cases the retrieval tests already covered). Diagnosed live with the user afterward: the fix wasn't taking effect in their browser at first -- turned out to be a stale dev server needing a restart + hard refresh, not a code problem (confirmed by re-running retrieval directly against their real exported profile/posting data, which found the expected candidates).
- 2026-07-24 — User-defined experience sections, per explicit request after noticing the printed resume merges "Work Experience" and "Extra-Curricular Activities" (as split on their original resume) into one hardcoded "Experience" heading. Added optional `ExperienceEntry.section` (mirrors `SkillGroup.category`'s existing pattern; unset/empty falls back to "Experience", so old profiles need no migration). `ExperienceForm.tsx` gained a "Section" text field per entry (new entries default to "Experience"); reused as-is by `ResumeEditor` since it shares the same form. `ResumePrintView.tsx`'s new `groupBySection` clusters entries by that field in first-appearance order and renders one heading per group instead of a single hardcoded "Experience" label; `selectResumeContent.ts` needed no change since it already spreads each entry (`section` passes through untouched). Build + typecheck + all 103 tests green (no new tests added -- this is a pure display grouping with no new branching logic beyond what the existing test suite's shape checks already cover). Also hand-edited the user's own exported backup JSON (`anti-funemployment-backup-2026-07-24-sections.json` in their Downloads folder, written via a one-off Node script, not committed) to add the correct `section` value to every experience entry in both the profile and the existing McDonald's generation, split exactly as their original PDF had it (6 entries → "Work Experience", 6 → "Extra-Curricular Activities") -- ready for them to re-import. Not yet verified live in the running app (no dev server access this session) -- next step is importing that file and confirming the printed resume shows two headed groups.
